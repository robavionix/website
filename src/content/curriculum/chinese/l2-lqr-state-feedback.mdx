---
title: "L2 — LQR 与状态反馈"
description: "跨入状态空间。为 L1 里那两种机体分别设计 LQR 调节器,并估计那些你无法直接测量的状态。"
date: 2026-01-02
icon: "Boxes"
level: 2
platform: "both"
targetStage: "本科三年级 / 硕士入门"
mathPrereq: "状态空间表示、线性代数"
tier: "core"
experiments:
  - platform: "fixed-wing"
    title: "固定翼 LQR"
    description: "在 FT-1 上处理配平、纵向动力学与横航向耦合——这次用的是状态空间形式,不再是 L1 那套频域 PID 够用的表示。设计一个纵向 LQR 控制器,拿去和 L1 里固定翼的 PID 级联,在同一个非线性模型测试上直接对比。"
  - platform: "fixed-wing"
    title: "估计 FT-1 测不到的状态"
    description: "为 FT-1 上无法直接测量的状态搭建一个 Luenberger 观测器——这正是 L5 中残差生成、进而实现故障检测的同一个思路,这里先在固定翼的状态空间模型上做一遍。"
  - platform: "fixed-wing"
    title: "在已知位置的舵面失效下稳住飞机"
    description: "把一侧副翼卡死在一个已知的偏转角上,看它彻底退出可控输入集——不是打折,是没了。检查剩下那侧副翼(现在是独立的,不再靠一个耦合的差动输入捆在一起)还能不能让降阶后的系统保持可控,围绕它重新设计 LQR 增益,然后会发现纯状态反馈消不掉一个稳态偏置——这个缺口正是引入积分作用的动机,也是 L5 用主动重构去补上的那个开口。"
  - platform: "quadrotor"
    title: "状态空间建模"
    description: "把线性化的四轴模型重新写成状态空间形式,建立哪些状态直接可控、哪些相互耦合的物理直觉。"
  - platform: "quadrotor"
    title: "LQR 设计"
    description: "调节 Q/R 权重矩阵,亲身感受状态偏差代价与控制能量代价之间的取舍,并与 L1 的 PID 在同一套故障上直接对比。"
  - platform: "quadrotor"
    title: "状态估计"
    description: "为四轴上无法直接测量的状态引入观测器(Luenberger 型)——这正是 L5 中残差生成、进而实现故障检测的同一个思路。"
---

L2 是 PID 与后续更一般控制理论之间刻意设置的桥梁:这是从传递函数跨入状态空间表示的自然一步,在 L1 已经引入的那两种机体上继续往下走。

## 一套理论,两架飞行器

学生要为四轴(以悬停为中心、结构对称)和 FT-1 固定翼测试平台(以配平为中心、纵向/横航向分离)设计同一套 LQR 框架。看到同一套控制理论应用在两种物理上完全不同的飞行器上,比记住任何一道标准答案都更有价值。

## 故障叙事的下一步

同一套标准故障库再次出现——四轴这边是电机推力衰减,FT-1 这边是升降舵/副翼故障——但这一次讨论的重点变了:LQR 的多回路联调可能在主轴上优于 PID,却可能悄悄让 PID 从未触及的耦合轴性能变差。这种对模型的依赖性,正是 L3 滑模控制要解决的弱点。
