---
title: "L1 — 经典 PID"
description: "建立飞行控制的第一直觉:在固定翼和四旋翼两种机体上分别设计并调试级联 PID 姿态控制器,看清楚这两条路径到达同一个结论的方式其实截然不同。"
date: 2026-01-01
icon: "Gauge"
level: 1
platform: "both"
targetStage: "本科二至三年级"
mathPrereq: "传递函数、频域分析"
tier: "core"
experiments:
  - platform: "fixed-wing"
    title: "建立并验证机体模型"
    description: "用 AVL 从 FT-1 的真实几何建立气动模型,在 Simulink 里配平并线性化。在信任这个线性化结果之前,用第二种独立方法交叉验证——这一级里的每一个控制器都建立在这个模型之上。"
  - platform: "fixed-wing"
    title: "单环姿态 PID"
    description: "设计一个直接的纯姿态 PID——俯仰和滚转分别直接闭环到升降舵/副翼——用 pidtune 在线性模型上调参。大部分时候它能工作,直到你仔细看:某个轴在每次指令之后要振荡几十秒都稳不下来,不管怎么重调那一个增益都解决不了。"
  - platform: "fixed-wing"
    title: "升级为角速度+姿态级联双环"
    description: "搞清楚为什么单纯重调单环增益解决不了这个振荡:它的修正信号要多经过一次积分才能作用到飞机真正的角速度上,这个额外的相位滞后是在激发这个轻阻尼模态,而不是在抑制它。把它重新搭成内环角速度+外环姿态的级联结构,稳态误差和振荡幅度都会收敛到指令的百分之几以内。"
  - platform: "fixed-wing"
    title: "用非线性飞机测试这个线性设计"
    description: "这套级联控制器完全是在一个小信号线性模型上设计出来的。把同一组增益接到 FT-1 完整的非线性动力学上闭环跑一遍,看看线性化做的那些假设,到底能保住多少干净的性能。"
  - platform: "fixed-wing"
    title: "带着失效的舵面继续飞"
    description: "在机动过程中注入升降舵部分失效或副翼卡死,记录这套从没被设计去检测故障的经典 PID 级联控制器会做出什么反应。这是之后每一级容错设计都要拿来对比的基线。"
  - platform: "quadrotor"
    title: "标定并验证机体模型"
    description: "从台架实测的推力/力矩系数出发,建立刚体+电机模型——这里用不上气动面板法,四旋翼的模型直接来自对硬件本身的标定。在信任它之前,先用真实推力台架数据验证。"
  - platform: "quadrotor"
    title: "角速度内环调试"
    description: "设计内环角速度 PID。把带宽、超调、稳态误差变成能亲手调出来的飞行语汇,而不是课本上的抽象名词。"
  - platform: "quadrotor"
    title: "升级为角速度+姿态级联双环"
    description: "四旋翼没有固定翼那种“先试试纯姿态、发现效果差”的奢侈条件——没有气动自阻尼,一个底下没有角速度反馈的姿态环,带来的不是曲线上的振荡,而是真实的摔机风险。先靠分析想清楚为什么,再上真实硬件搭外环姿态控制,而且是带着已经想清楚的理由去搭。"
  - platform: "quadrotor"
    title: "用非线性机体测试这个线性设计"
    description: "这套级联控制器是在小角度线性化上调出来的。把它推进大角度机动,让线性模型忽略掉的欧拉角奇异性和陀螺耦合非线性开始起作用,看看真实的裕度到底在哪里。"
  - platform: "quadrotor"
    title: "第一次故障注入"
    description: "现场注入单电机推力衰减 60% 与 IMU 噪声突增。精确记录 PID 能撑到哪个边界才失稳——也为 L3/L5 埋下伏笔。"
---

Robavionix 平台上每一级课程都跑在同一套标准故障库上。L1 是这一切开始的地方:一个手动调试的级联 PID 姿态控制器,在两种物理上截然不同的机体上分别搭建,各自以自己的方式被推到极限、失效。

## 为什么从这里开始

控制类教学的失败,往往不在内容深度,而在学生某一级数学台阶没跨过去就被拖着走。L1 只假设传递函数与频域分析——没有状态空间,没有 Lyapunov 函数。目标是第一堂课就在两种机体上都跑起一个能工作的控制器,并且精确地、亲身地回答一个问题——而且这个问题的答案在两种机体上其实并不一样:**PID 控制器到底能被推到多远才会放弃?这个极限,在四旋翼上和在固定翼上,是同一个极限吗?**

## 你将搭建什么

FT-1 这边:角速度+姿态级联结构,但设计对象是一个先用两种独立方法交叉验证过、再拿去调参的 AVL/Simulink 气动模型,调完还要拿到完整非线性飞机上压力测试一遍,才能说它“能用”。四旋翼这边:同样是每个轴上的角速度 PID,再叠加外环姿态控制,烧录到 STM32 飞控板上,在 HIL 实验台的真实四轴机体上运行——不是纯仿真。

## 故障叙事

本级每个实验都以同一个动作收尾:注入标准故障库里的一种——四旋翼这边是电机推力衰减或卡死,FT-1 这边是升降舵部分失效或副翼卡死——记录发生了什么。重点不是让 PID 出丑,而是给“鲁棒裕度”这个词一个学生亲手测出来的精确基线——这样当 L3 引入滑模控制去对付同一批故障时,那份提升是他们自己测量出来的,而不是课件上的一句断言。
