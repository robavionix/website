---
title: "L2 — LQR & State Feedback"
description: "Cross into state-space control. Design an LQR regulator for both airframes from L1, and estimate the states you can't measure directly."
date: 2026-01-02
icon: "Boxes"
level: 2
platform: "both"
targetStage: "Undergraduate year 3 / MSc entry"
mathPrereq: "State-space representation, linear algebra"
tier: "core"
experiments:
  - platform: "fixed-wing"
    title: "Fixed-wing LQR"
    description: "Trim, longitudinal dynamics, and coupled roll-yaw on FT-1, now handled in the state-space form L1's frequency-response PID didn't need. Design an LQR longitudinal controller and compare it directly against L1's fixed-wing PID cascade on the same nonlinear-model test."
  - platform: "fixed-wing"
    title: "Estimate what FT-1 can't measure"
    description: "Build a Luenberger observer for FT-1's states that aren't directly sensed — the same residual-generation idea that becomes fault detection in L5, worked out here on the fixed-wing state-space model."
  - platform: "fixed-wing"
    title: "Stabilize through a known actuator loss"
    description: "Jam one aileron at a known deflection and watch it drop out of the controllable input set entirely — not degraded, gone. Check whether the surviving aileron (independent now, not coupled through one differential input) keeps the reduced system controllable, redesign the LQR gain around it, and find the steady-state offset pure state feedback can't null. That gap is what motivates integral action — and it's the opening L5 walks through with active reconfiguration."
  - platform: "quadrotor"
    title: "State-space modelling"
    description: "Rebuild the linearised quadrotor model in state-space form and build physical intuition for which states are directly actuated and which are coupled."
  - platform: "quadrotor"
    title: "LQR design"
    description: "Tune the Q/R weighting matrices and feel the state-error-vs-control-effort trade-off directly, compared side by side against L1's PID on the same fault set."
  - platform: "quadrotor"
    title: "State estimation"
    description: "Introduce an observer (Luenberger-style) for quadrotor states you can't measure directly — the same residual-generation idea that becomes fault detection in L5."
---

L2 is the deliberate bridge between PID and the more general control theory that follows: the natural jump from transfer functions to state-space representation, worked through on the same two airframes L1 already introduced.

## Two controllers, one theory

Students design the same LQR framework for both the quadrotor (hover-centric, symmetric) and FT-1, the fixed-wing testbed (trim-centric, longitudinal/lateral-directional split). Seeing one control theory applied to two physically different vehicles is worth more than any amount of correct answers memorised in isolation.

## Where the fault story goes next

The same standard fault library returns — motor thrust loss on the quadrotor, elevator/aileron faults on FT-1 — but now the discussion is different: LQR's multi-loop tuning can outperform PID on the primary axis while quietly degrading performance on cross-axes the single-loop PID never touched. That model-dependence is exactly the weakness L3's sliding mode control is built to answer.
